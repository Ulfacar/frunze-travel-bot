# СПЕКА для Codex — предпрод-фиксы (по ревью) + почта + аккаунты менеджеров

Автор: Claude (ревьюит). Исполнитель: Codex. Ветка: текущая `feature/token-economy-phase1`. Не деплоить/не пушить/не коммитить.

Контекст: перед проливом на прод закрываем находки ревью и готовим боевые аккаунты менеджеров. Прод — живой WhatsApp-бот, поэтому корректность критична.

## TASK A — Убрать почту GetVisa из ответов бота (константу оставить)
Убрать использование `GETVISA_EMAIL` из клиентских ответов/промптов, **саму константу в `app/core/branding.py:18` НЕ удалять** (оставить на будущее). Затронуть:
- `app/core/branding.py:96` — убрать строку/значение «Почта для док-в» (или убрать email из ответа) так, чтобы бот не называл почту.
- `app/agent/prompts/visa.py:53` — убрать `{GETVISA_EMAIL}` из системного промпта визы (оставить адрес).
- `app/agent/runner.py:212` — убрать упоминание почты из ответа (переформулировать без email).
- `app/funnels/visa.py:59` — убрать «Документы можно прислать на {GETVISA_EMAIL}» (переформулировать: документы/детали уточнит менеджер, без почты).
- `tests/test_calibration.py:31` — тест ассертит `GETVISA_EMAIL in text`; обновить: теперь почты в ответе быть НЕ должно (заменить на assert, что email отсутствует, либо убрать проверку email).
Убрать неиспользуемые импорты `GETVISA_EMAIL`, где он больше не нужен.

## TASK B — Fix авторизации скоупа менеджеров (ревью major #1/#2) + боевые аккаунты
Проблема: `_manager_bot_scope` (`app/admin/router.py:179-188`) при отсутствии записи возвращает `None`, а `_can_view_conversation` трактует `None` как «видит всё» (fail-open). Любой менеджер, добавленный через env `MANAGERS` без записи в `BOT_SCOPE_BY_MANAGER`, увидит ВСЕ диалоги.

Сделать:
1. **Добавить в `BOT_SCOPE_BY_MANAGER`** записи для визовых менеджеров: `medina -> getvisa`, `eliza -> getvisa` (и по имени: `Медина -> getvisa`, `Элиза -> getvisa`, т.к. lookup идёт по login ИЛИ name). Убедиться, что уже есть `ademi -> frunze_tours`, `sezim -> frunze_tours_sezim`.
2. **Fail-closed:** изменить логику так, чтобы НЕизвестный НЕ-админ менеджер (нет записи в скоуп-карте) видел **ничего чужого**, а не всё. Админ (`settings.admin_user`, а также существующий admin-паттерн) продолжает видеть ВСЁ. Т.е. `None`-scope для обычного менеджера должен трактоваться как «пустой доступ» (видит только явно свой скоуп; если скоупа нет — не видит чужие карточки, прямой URL → 404). Не сломать текущее поведение admin=всё и ademi/sezim=свой бот.
3. Согласовать демо-профили (`_demo_profiles`) с реальными аккаунтами: логины `ademi/sezim/medina/eliza` и их имена/скоуп не должны конфликтовать (в ревью отмечено странное «Медина→GetVisa» в демо — привести к консистентности: medina и eliza → скоуп getvisa).
4. Тесты (`tests/test_panel.py`): добавить/дополнить — medina и eliza видят только диалоги `getvisa`, не видят tours-диалоги (прямой URL → 404); **неизвестный менеджер без записи в скоупе НЕ видит чужие диалоги** (fail-closed); admin видит всё. Существующие тесты Адеми/Сезим (40 passed) не ломать.

## TASK C — Не кэшировать волатильный suggest_reply (ревью major #3)
Системный промпт `suggest_reply` (`app/admin/router.py` ~800-826) содержит `conv.ai_summary`/`manager_next_step` — меняется почти каждый вызов, поэтому `cache_control` даёт только write-premium без hit. В пути `chat()`/`suggest_reply` НЕ оборачивать system в `cache_control` (например, флагом `cacheable=False` в `_system_content_for_model`/`create`, по умолчанию поведение боевых воронок не менять). Боевой роутинг воронок (кэш статичного промпта) оставить как есть.

## TASK D — Сузить широкие FAQ-паттерны (ревью minor #4)
В `app/core/faq.py` в новых визовых правилах:
- Правило «Сроки оформления визы»: убрать одиночный паттерн `делается` (слишком широкий). Заменить на более специфичные, НЕ содержащие ценовых слов: например `срок оформлен`, `сроки оформлен`, `как долго делают визу`, `когда будет готова виза`, `сколько идет виза` (без слова «сколько стоит»; помнить про `PRICE_INTENT_RE`, где есть `сколько` — не использовать голое «сколько», только в связке, не дающей ложного ценового матча... на самом деле любой `сколько` перехватит visa_pricing, поэтому НЕ использовать паттерны со словом «сколько» вообще). Итог: паттерны про срок без слов `сколько/цена/...`.
- Правило «Что входит в сопровождение»: убрать/сузить `что вы делаете` (широко) — оставить `что входит`, `что включает`, `что входит в услугу`, `чем помогаете`, `какая помощь по визе`.
Не создавать ничьих приоритетов; funnel="visa" сохранить.

## TASK E — Nits
- Удалить мёртвую `_filter_card_models` (`app/admin/router.py:205-212`), если действительно не используется (проверить grep).
- В `chat()`/`suggest_reply` (`app/agent/llm.py:116-133`) для генерации черновика передавать `tools=None`/`[]` (черновик менеджеру tool-calls не делает) — экономия токенов.

## Verification
`PYTHONPATH=. pytest -q` — весь набор зелёный (сейчас 184). Особо: тесты панели на скоуп (включая fail-closed и medina/eliza) и тест калибровки без почты. В отчёте перечислить изменённые файлы и результат pytest.
