# Пакет приёмки — Sprint 1 (календарь, задачи, Telegram Morning Brief)

**Для:** GPT-5.6 (приёмка пакета по цепочке ролей: Алан → Fable → Claude → **GPT-5.6** → Opus)
**Дата пакета:** 22.07.2026
**Ветка:** `feat/calendar-tasks-morning-brief` (HEAD `9f25c91`), базируется на `main`
**Статус:** реализовано, запушено, НЕ смёржено, НЕ задеплоено

---

## 1. Скоуп пакета (что принимаем)

Ветка несёт четыре слоя работы (снизу вверх):

| Слой | Коммиты | Что это |
|---|---|---|
| Дожим + ценовая вилка | `0bc2a96`, `a4edd5a` | Промпт-блок `DOZHIM_AND_PRICE_FORK`, второе касание, режим «усталость→суть». Флаг `dozhim_enabled`, дефолт OFF |
| WP1M/WP1B — фундамент | `57f4daf`…`9cae9f6` | Request-correlation, network-guard в тестах, alembic-фундамент, доменная схема (6 таблиц), сервисный слой, shadow-bridge за флагом |
| WP2/WP3 — messaging/authz | `e2c6b1f`, `636fe83` | Канонические сообщения, inbox/outbox, dedup, анти-loop, authz-каркас |
| **Sprint 1 — календарь** | `a7eb983`, `9f25c91` | Домен CalendarTask/CalendarTaskEvent, UI `/admin/calendar` (день/неделя), блок «Задачи» в карточке клиента, персональный Telegram Morning Brief per-manager |

Диффстат к main: **62 файла, +7194/−28**, из них ~2000 строк — новые тесты (14 новых тест-файлов).

## 2. Ключевые архитектурные решения

- **Alembic-миграции** (4): `wp1a_domain_0001` → `wp1b_dialog_uidx_0002` → `wp2_messages_0003` → `sprint1_calendar_0004` (head). Легаси-таблицы прода миграции НЕ трогают (проверено тестом `test_upgrade_does_not_create_any_legacy_table`).
- **Все новые фичи за флагами, дефолт OFF:** `domain_shadow_enabled`, `messaging_shadow_enabled`, `calendar_brief_enabled`, `dozhim_enabled`. Деплой ветки сам по себе ничего не включает.
- **Часовой пояс:** Bishkek UTC+6 захардкожен в календарной логике.
- **Владение задачами** по `manager_id` (login); overnight-лиды в брифе роутятся владельцу (`9f25c91`).
- **Contact find-or-create по телефону** — только при явном создании задачи; shadow-режим контакты НЕ создаёт.
- **Telegram-бриф:** `ManagerConfig.telegram_chat_id` только из env, не логируется; без chat_id — бриф только на экране, в общую группу НЕ падает.
- **Bitrix не тронут**, source-of-truth не переключался.

## 3. Свидетельства тестирования

| Прогон | Результат | Когда |
|---|---|---|
| Полный сьют, SQLite | 476 passed | 18.07 (коммит-гейт) |
| **Полный сьют + PostgreSQL 16** (`TEST_POSTGRES_DSN`, throwaway-контейнер postgres:16-alpine) | **478 passed, 1 skipped** (скип = сетевой guard-тест, намеренный) | **22.07** |
| PG-тир alembic: upgrade→downgrade→reupgrade + partial unique indexes/типы на живом PG | 12/12 passed | 22.07 |

**Гейт «PostgreSQL-валидация до prod» — ЗАКРЫТ 22.07.**

## 4. Состояние прода (аудит 22.07, read-only)

- Прод = main от 11.07, image `1be488a9ca8e`, healthy, uptime 10 дней.
- Из этой ветки на проде НЕТ НИЧЕГО: ни alembic, ни domain-слоя, ни календаря. `alembic_version` в БД отсутствует.
- Прод-БД: PostgreSQL 16 (совпадает с валидированной версией), 6 легаси-таблиц, 1191 conversations / 9672 messages / 135 deals.
- Интеграции живые: Wappi inbound идёт, Bitrix-зеркало 77 попыток / 0 ошибок за 24ч, scheduler без ошибок.

## 5. Известные ограничения / открытые вопросы

1. **Визный пинг второго касания**: формулировка «при отказе вторую попытку делаем бесплатно» ЗАМЕНЕНА на нейтральную (22.07, uncommitted) до подтверждения Гришей — политика «без гарантий». Вопрос Грише: даёт ли GetVisa бесплатную вторую попытку при отказе?
2. Дожим: формулировки утверждены Аланом 22.07 (кроме п.1); прогон тренажёра «дорого → офис» ещё не делался.
3. TG-бриф не проверялся на живом chat_id (нужны telegram_chat_id менеджеров в env).
4. `main.py.bak.` лежит в прод-образе (мусор старого деплоя, к ветке отношения не имеет).

## 6. Что просим от приёмки (GPT-5.6)

- Ревью диффа `main...feat/calendar-tasks-morning-brief` на: корректность authz-гейтов (перехват запрещён, кроме админа), идемпотентность миграций, отсутствие утечек chat_id/токенов в логи, таймзоны.
- Подтвердить: деплой с флагами OFF безопасен (ни одна новая фича не активируется сама).
- Вердикт: ACCEPT / ACCEPT-с-замечаниями / REJECT + список блокеров.

## 7. План деплоя после приёмки (набросок, НЕ выполнять)

1. Бэкап прод-БД (pg_dump) + бэкап файлов приложения (по ранбуку).
2. Деплой файлов ветки, `alembic upgrade head` (создаст только новые таблицы).
3. Рестарт compose строго с `-f docker-compose.vps.yml --env-file prod.env`.
4. Smoke: health, /admin/calendar открывается, легаси-потоки (Wappi, зеркало) не задеты.
5. Флаги остаются OFF; включение — отдельным решением на малой группе.
6. Rollback: вернуть бэкап файлов, `alembic downgrade base` (тестами доказано, что сносит только новые таблицы), рестарт.
