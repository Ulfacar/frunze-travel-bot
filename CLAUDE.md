# Frunze Travel Bot — контекст проекта

Этот файл Claude Code подхватывает автоматически на любом устройстве. Он заменяет
локальную память (она не переезжает между машинами) и держит то, что **не выводится
из кода и git-истории**. Актуальность — 23.09.2026.

---

## 1. Что это

AI-агент-продавец для турагентства в Бишкеке. Принимает клиента в WhatsApp/Telegram,
квалифицирует, подбирает туры через TourVisor, отвечает по визам, передаёт «тёплого»
менеджеру и зеркалит всё в Bitrix24.

**Два бренда, один код:**
- **Frunze Travel** — туры и авиабилеты (номер +996707660009, профиль Wappi `FrunzeTravel2`)
- **GetVisa** — визы (номер +996706660009, профиль Wappi `GetVisa`)

Бот **в боевой эксплуатации с 01.07.2026**. Считать реальные диалоги только с этой даты —
всё, что раньше, это обкатка с выключениями.

**Люди:** Алан (`Ulfacar`, arialvhs@gmail.com) — ведёт проект, с ним и разговариваем.
Даулет — владелец бизнеса и заказчик по турам. Гриша — заказчик по визам (GetVisa).
Менеджеры в скоупе: Адеми, Айсина, Медина, Элиза (Сезим уволилась 29.07 → её канал
представляется Айсиной). Марсель — отдельная ветка рабочих виз, свой бот, пока отложена.

---

## 2. Как работать с Аланом (протокол, важнее всего остального)

**Доказательство важнее уверенности.** Предыдущий агент мог ошибиться, прошлый отчёт мог
устареть, исполнитель не судья собственной работы. Наш повторяющийся провал — не баги, а
отчёты, которые врут.

1. **Диагностику даю сразу**, не спрашивая разрешения посмотреть.
2. **Чиню целиком**, а не по кусочку — но **на прод выкатываю только по прямому слову Алана**.
   Само по себе «почини» деплой не разрешает.
3. **Всё, что меняет поведение для клиента, включается флагом, и флаг по умолчанию OFF.**
   Выкатили код — поведение не изменилось; включаем отдельным решением.
4. **Называю класс риска до начала работы** и веду задачу по нему:
   **S** — текст, лог, тест, док: сделал → `pytest -q` → показал дифф.
   **M** — несколько файлов, клиента не касается: гейт до кода → `pytest -q` +
   `prod_traps_check --diff-base HEAD` → ревью по диффу.
   **B** — поведение бота (промпт, воронка, ответ клиенту): M + флаг OFF + замер на реальных данных.
   **C** — миграции, деньги, рассылки, ключи, прод-данные, детекторы с порогами: B +
   независимое ревью (`codex-reviewer`) + калибровка на истории (`alert_replay.py`) + решение Алана.
   Сомневаюсь между классами — беру старший.
5. **Границы дифа объявляю до работы**, после — сверяю `git diff --stat`. Каждый лишний файл
   объясняю. Расширять задачу, «раз уж я здесь», нельзя: замеченное предлагаю, а не делаю молча.
6. **`/health` — не доказательство.** Различаю: `PROD_HEALTHY` (контейнер поднялся) и
   `PROD_FUNCTIONALLY_VERIFIED` (функция вызвана на проде и вернула то, что должна). После
   деплоя обязан сделать второе — вызвать изменённое в боевом контейнере и показать вывод.
   `smoke_test.py` по проду не гонять: он создаёт мусорный диалог через вебхук.
7. **Отчёт заканчиваю доказательствами:** что сделано / где (файлы, коммит) / чем проверено
   (команда и число) / где проверено (локально, образ, прод) / **что осталось непроверенным**.
   «Работает», «тесты зелёные», «деплой прошёл» — не доказательства. Не мерил — так и говорю.
8. **Сверяю диф, а не отчёт агента.** Был случай: отчёт заявил предохранители, которых в
   коде не было. Перед любым прод-действием читать диф/хеш.
9. **Статусы требований:** `DONE / PARTIAL / BROKEN / MISSING / BLOCKED / UNKNOWN`. Работающее
   не переписываю без доказательства регрессии; `UNKNOWN` превращаю в факт проверкой, не догадкой.
10. **Стоп-слова.** `NEEDS_HUMAN_DECISION` — бизнес-политика не определена, деньги, необратимое,
    архитектурный выбор меняет продукт. `BLOCKED` — доказательство получить нельзя, состояние
    прода противоречиво. Молча выбирать продуктовое решение за Алана запрещено.
11. **Не добавляю лишнего.** Не требуется задачей, не нужно для корректности, не нужно сейчас —
    не делаю, записываю как долг.
12. Язык — русский, неформальный. Готовые тексты и решения ценятся выше вариантов на выбор.
13. На пересланные скриншоты переписки — сначала **разбор ситуации**, готовый текст ответа
    одной строкой в конце, а не вместо разбора.

Полный процесс — `docs/protocol.md`, скилл `/protocol`. Триггеры: «протокол», «по протоколу».
Прежний процесс «Веном v2» (`docs/venom-v2.md`) — архив, не используется с 07.09.2026; его
гейт до работы, чек-лист прод-ловушек и калибровка детекторов перенесены в новый протокол.

---

## 3. Карта кода

```
app/
  main.py            FastAPI: вебхуки /webhook/{telegram,wappi,bitrix}, /health, роутеры
  config.py          настройки из env (prod.env на сервере)
  agent/             LLM-ядро: runner, routing (Haiku/Sonnet), prompts/, validator, facts, tools
  funnels/           три воронки: tours.py, visa.py, tickets.py
  channels/          адаптеры каналов: wappi.py (WhatsApp), telegram.py, bitrix_openlines.py, outbound.py
  core/              вся операционная логика (~50 модулей), самое живое:
                       orchestrator.py    вход сообщения → состояние → ответ
                       state.py           состояние диалога, ключ bot_id:phone
                       flags.py           рантайм-флаги (см. §6)
                       readiness.py       триаж лида в тиры «покупатель сегодня»
                       intercept.py       перехват диалога менеджером (бот замолкает)
                       followup.py        дожим
                       schedule.py        часы приёма (см. §7)
                       stt_guard.py       фильтр доверия к расшифровке голосовых
                       channel_heartbeat.py / wappi_health.py  сторож каналов
                       morning_brief.py / calendar_brief.py    утренние сводки менеджерам
                       bitrix_pipeline_job.py, manager_sync.py  связка с CRM
  domain/            доменный слой: models, services, assignment_queue, autoassign, permissions
  integrations/
    crm/             bitrix24.py, bitrix_mirror.py, bitrix_pipeline.py, postgres.py, stub.py
    tourvisor/       client.py (XML API), cards.py (карточки туров), quota.py
    stt/             openai_stt.py + registry/service
    panel/           данные для админки: buyers, analytics, store
  admin/             админ-панель (Jinja-шаблоны, Basic-auth, канбан диалогов)
  web/offers.py      публичные страницы подборки туров /t/<id>
tests/               136 файлов, 1687 тестов
scripts/             deploy_prod.sh, rollback_prod.sh, prod_traps_check.py, sim_tour_card.py,
                     run_scenarios.py (тренажёр качества), smoke_test.py, tourvisor_probe.py
alembic/             миграции
docs/                планы, ТЗ, разборы — см. §9
```

**Тесты:** `python -m pytest tests -q` (именно `tests`, не корень — в `runs/` лежат
временные каталоги, на которых сборка падает по правам на Windows). Гейт по PG:
тесты проходят и на SQLite, и на PG16.

---

## 4. Прод и деплой

- Сервер: **62.171.185.155** (Ubuntu 24.04), SSH root по ключу. **Личный сервер Алана**,
  не заказчика. Путь `/root/frunze-travel`.
- Стек: docker compose — `app` (uvicorn на 127.0.0.1:8077) + postgres16 + redis7.
  Наружу смотрит **системный** nginx с сертификатом Let's Encrypt, домен `frunzetravel.kg`
  (плюс `app.frunzetravel.kg` под будущее Mini App).
- Docker — snap-версия: работает только из `/root` и не читает скрытые файлы,
  поэтому `.env` называется **`prod.env`** и он **вне git** (в нём боевые токены).

**Деплой — только через скрипт, руками compose не поднимать:**

```bash
# локально: отдать код на сервер (прод тянет НЕ с GitHub, а со своего bare-репо)
git push prod <branch>
# на сервере:
/root/frunze-travel/scripts/deploy_prod.sh <branch>     # дефолт: fix/tours-search-quality
/root/frunze-travel/scripts/rollback_prod.sh [commit]   # откат, если поломка логическая
```

`deploy_prod.sh` сам бэкапит prod.env, ждёт `/health` и **откатывается**, если приложение
не поднялось. Три ловушки, зашитые в скрипт и стоившие нам инцидентов:
1. `docker compose` без `--env-file prod.env` → `MANAGERS` пустой, логины менеджеров в
   панель отваливаются **молча**, при этом `/health` зелёный.
2. Без `-f docker-compose.vps.yml` контейнер лезет на 80/443, где уже сидит системный nginx.
3. `-f docker-compose.vps.yml` **без базового** `-f docker-compose.yml` → «service "app" has
   neither an image nor a build context». Рабочая форма (22.09 наступили на это, поднимая
   контейнер руками после подмены ключа):
   `docker compose -f docker-compose.yml -f docker-compose.vps.yml --env-file prod.env up -d app`

**Гит-ремоуты:** `origin` = GitHub (`Ulfacar/frunze-travel-bot`), `prod` =
`ssh://root@62.171.185.155/root/frunze-repo.git`. Рабочая ветка — `fix/tours-search-quality`,
`main` держим на том же коммите.

---

## 5. Внешние системы и их подводные камни

**TourVisor** (подбор туров, XML API, аккаунт `frunzetravel@mail.ru`)
- Из Бишкека реально вылетают **Турция, ОАЭ, Мальдивы, Азербайджан**. По Египту, Таиланду,
  Кипру поиск честно возвращает **0 отелей** — и бот раньше врал, что дело в бюджете.
- Обязательны `childage1..N` — без возрастов детей выдача пустая.
- Валюта в ответах плавает (EUR), звёздность отелю LLM недоступна — берём из полей.
- Есть квота (`quota.py`); тариф меняли, «Горящие туры» — отдельный платный пакет.

**Wappi** (мост WhatsApp) — каналы регулярно отваливаются по QR и это **не видно по трафику**:
тишина не отличает мёртвый канал от тихого. Поэтому сторож v3 спрашивает у Wappi
**статус профиля**, а не считает молчание. Подписки платные, у GetVisa истекала 20.08.2026 —
проверить оплату.

**Bitrix24** (`getvisakg.bitrix24.kz`, inbound REST-вебхук; токен НЕ хранить в репо/памяти)
- **Два потока лидов, это корень жалобы «мы не видим бота»:** поток Wappi → Открытые линии
  (там сидят менеджеры) и поток нашего бота (лиды падали на служебный аккаунт 155383).
- Реплики бота попадают в чат Открытой линии только с параметром **`bot_id=external_api`**.
  Ответы менеджеров возвращаются через `outgoing_message_api`.
- ID менеджеров: Медина 96451, Элиза 110841, Адеми 155313, Айсина 155267.
- Разница поведения виз и туров = **тип очереди Открытой линии** (`evenly` против `all`).
- Портал молча съедает эмодзи и BBCode в карточках — это дефект портала, не наш.
- 🔴 **Продажи в CRM не заносятся с 13.06** → конверсия объективно не меряется. Любые
  выводы про «сколько продали» через Bitrix недостоверны: у таргетологов 158 продаж
  против 7 сделок в CRM.

**LLM** — OpenRouter, роутинг Haiku/Sonnet + кэш. Расход порядка **$17/мес**.

**STT (голосовые)** — OpenAI. Технически 100% успех, ~28 голосовых/день. Но **кыргызского
языка в модели нет**: 54% кыргызской речи — нечитаемая каша. Отсюда `stt_guard` — фильтр
доверия к расшифровке.

---

## 6. Рантайм-флаги

Переключаются кнопкой в админке, живут в БД (`app_flags`), переживают рестарт, дефолт
берётся из env. Ключи (`app/core/flags.py`, `get_flag`):

```
bots_enabled              общий рубильник ботов
night_mode_enabled        per-bot: туры круглосуточно, визы ночью
dozhim_enabled            дожим + ценовая вилка
followup_enabled          автодожим
alerts_enabled            алерты
morning_brief_enabled     утренний брифинг
calendar_brief_enabled    календарь звонков
readiness_rescore_enabled пересчёт тиров лидов
outcome_infer_enabled     ИИ-определение исхода диалога
stt_enabled / stt_guard_enabled / stt_public_enabled     голосовые
tours_cards_enabled / tours_summary_enabled / tour_facts_enabled  подача туров
bitrix_mirror_enabled / bitrix_pipeline_enabled / bitrix_autodeal_enabled /
bitrix_prefer_openline_lead                              связка с CRM
channel_heartbeat_enabled / wappi_health_enabled         сторож каналов
```

---

## 7. Бизнес-правила, которых нет в коде очевидно

- **Часы приёма:** разговаривать бот может в любой день, а **визит в офис назначать только
  пн–сб**. Это перекрывает «работаем ежедневно» из туровой анкеты — бот однажды записал
  клиента на воскресенье.
- **Визы:** бот называет официальный прайс (250$/100€ и т.д.). Депозиты, проценты и любые
  гарантии выдачи — **запрещены**. По «нашим» странам консультирует сам, по чужим
  (Бразилия и пр.) сразу отдаёт менеджеру.
- **Цены по турам** публикует только админ; клиенту — вилка как приманка в офис.
- **Ручное важнее бота:** менеджер перехватил диалог → бот замолкает (`intercept.py`, TTL).
- **Владение диалогом закреплённое**, перехват чужого запрещён (кроме админа).
- **Бот — ночной диспетчер, а не продавец.** Это рефрейм всей ценности: медиана ответа
  живого менеджера была 3 часа, 20% лидов приходят ночью.
- Бот знает сегодняшнюю дату — её инъектим в промпт каждый ход (без этого он гадал месяц).

---

## 8. Состояние на 23.09.2026

- `main` и `fix/tours-search-quality` синхронны, HEAD `9138bdb`, 1687 тестов.
- 🔴 **Бот молчал шесть дней, 17–22.09.** На счёте OpenRouter кончились деньги, ключ отдавал
  `402 Payment Required` — 2877 отказов, из них 178 живых клиентских ходов. Вылечено подменой
  `OPENROUTER_API_KEY` в `prod.env` 22.09. Сторож баланса предупредил **за сутки**, 16.09,
  и повторял каждое утро — алерт был, реакции не было. Урок не в детекторе, а в том, что
  одинаковое ежедневное уведомление перестают читать.
- Деньги: остаток порядка **$10**, реальный расход **~$0.32/сутки** (не $17/мес, как считалось).
  `LLM_DAILY_BUDGET_USD=10` — суточный лимит выше всего остатка, то есть предохранителя
  фактически нет.
- Ночной `403` Битрикса: 869 отказов 20–22.09, строго в окно 03:00–07:00, при живом вебхуке
  (`profile.json` → `ADMIN: true`). 23.09 не повторился ни разу. Причина **UNKNOWN**; в коде
  оставлена ловушка — `bitrix24.py` логирует тело ответа портала перед `raise_for_status`.
- Перехват менеджерами — половина базы (1832 беседы из 3627). Бот в таких диалогах молчит
  штатно; тишина в логах после рестарта объясняется этим, а не поломкой.

**Открытые хвосты:**
- 🔴 **Трафик виз упал втрое с 20.09** (70–140 клиентских сообщений в день → 25–32), и исчез
  именно ночной — тот, где по визам работает бот (днём 10:00–19:00 его очередь не наступает).
  23.09 сторож дал `CHANNEL DOWN: getvisa (тишина 1273 мин, диагноз=webhook, профиль=здоров)`.
  Идёт замер: теряем сообщения по дороге или их правда не шлют.
- ⚠️ **Оплата Wappi по туровым профилям до 01.10.2026**, у `getvisa` до 23.10.
- Продажи в Bitrix не заносятся с 13.06 — метрика конверсии слепая.
- Владение кодом на бумаге не зафиксировано, бот Марселя (рабочие визы) отложен,
  воронка «Билеты» тонкая.

---

## 9. Что в репозитории устарело — не верить

- **`README.md`** — пишет «MVP-скелет, CRM заглушка, канал Telegram». Неправда с июля:
  боевая эксплуатация, живой Bitrix24, основной канал WhatsApp через Wappi.
- **`DEPLOY_STATUS.md`** — верен по топологии (nginx, snap-docker, prod.env), но описывает
  деплой копированием файлов. С 15.08 деплой — git-checkout скриптом (см. §4).
- `PROJECT_CONTEXT_2026-07-02.md`, `HANDOVER.md` — снимки июля, читать как историю.

**Актуальное читать здесь:** `docs/project-journey.md` (**полный путь проекта: хронология,
грабли по системам, уроки — читать первым, если садишься за проект впервые**),
`docs/services-and-models.md` (внешние сервисы, выбор моделей, деньги, чек-лист «бот странный»),
`docs/HANDOVER-ACTUAL-2026-08-03.md` (подробный разбор
состояния), `docs/protocol.md` (процесс; `venom-v2.md` — архив), `docs/module-stt-voice-2026-08-04.md`,
`docs/task-*.md` (ТЗ по модулям), `docs/plan-avgust-3007.md` (рабочий план августа).

---

## 10. Мелочи, которые экономят время

- В корне лежат неотслеживаемые рабочие файлы (`*.m4a`, `bitrix_report.txt.txt`, docx/pptx) —
  это артефакты, в git их не тащим.
- У Алана на ПК работает мост Telegram → Claude Code (`claude-tg-bridge`, бот
  @FrunzeHelper_bot) — команды могут приходить оттуда.
- Терминал в среде Алана иногда глючит: вывод команд перепроверять явно.
- Есть тренажёр качества `scripts/run_scenarios.py` (прогон сценариев через настоящий бот
  с судьёй) и симулятор карточек `scripts/sim_tour_card.py` — они уже опровергали мои
  выводы «на глаз», пользоваться до споров, а не после.
